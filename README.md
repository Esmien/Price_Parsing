# 📜 Telegram Price Parser to Google Sheets

Автоматизированный ETL-пайплайн для сбора, обработки и экспорта прайс-листов из Telegram-каналов в Google Таблицы.

## 🚀 Возможности

*   **Мульти-источники:** Сбор данных из нескольких постов Telegram и объединение их в одну категорию (например, разные посты iPhone 13, 14, 15 → один лист "iPhone").
*   **Умный парсинг:**
    *   Распознает флаги стран (даже если они на другой строке или в виде Emoji 🇦🇪).
    *   Корректно извлекает цены в разных форматах (`8.700`, `115 000`, `1000`).
    *   Очищает названия и комментарии от мусора.
*   **Google Sheets Integration:** Автоматически создает листы в таблице и обновляет данные.
*   **Локальный бекап:** Сохраняет историю парсинга в JSON-файлы.
*   **Модульная архитектура:** Легко расширять и поддерживать.

## 🛠 Технологии

*   **Python 3.10+**
*   **Requests & BeautifulSoup4** — скрапинг данных.
*   **GSpread** — работа с Google Sheets API.
*   **Python-dotenv** — управление конфигурацией.
*   **Dataclasses** — типизация данных.

## ⚙️ Установка и запуск

### 1. Клонирование репозитория
```bash
git clone https://github.com/Esmien/price-parser.git
cd price-parser
```

### 2. Настройка окружения
Создайте виртуальное окружение и установите зависимости:
```bash
python -m venv venv
source venv/bin/activate  # Для Linux/macOS
venv\Scripts\activate     # Для Windows

pip install -r requirements.txt
```

### 3. Настройка Google Cloud
Для работы с таблицами нужен сервисный аккаунт:
1.  Создайте проект в [Google Cloud Console](https://console.cloud.google.com/).
2.  Включите **Google Sheets API** и **Google Drive API**.
3.  Создайте Service Account, скачайте JSON-ключ и переименуйте его в `credentials.json`.
4.  Положите `credentials.json` в корень проекта.
5.  **Важно:** Откройте вашу Google Таблицу и дайте доступ **Редактор** для email-адреса, который указан в `credentials.json` (`client_email`).

### 4. Конфигурация (.env)
Создайте файл `.env` в корне проекта:
```ini
# Имя файла с ключами (лежит в корне)
CREDENTIALS_FILE=credentials.json

# Название вашей Google Таблицы (должно совпадать точь-в-точь)
SPREADSHEET_TITLE=TableTitle
```

### 5. Настройка ссылок (config.py)
Откройте `config.py` и настройте `URL_MAP`. Вы можете добавлять сколько угодно категорий и ссылок:
```python
URL_MAP = {
    "Huawei": ["https://t.me/ChannelName/123?embed=1"],
    "Samsung": [
        "https://t.me/ChannelName/456?embed=1",
        "https://t.me/ChannelName/457?embed=1"
    ]
}
```

### 6. Запуск
```bash
python main.py
```

## Техническое описание

Этот раздел описывает архитектуру, ключевые компоненты и логику работы парсера.

### Инициализация

Перед запуском скрипта происходит начальная настройка:
- **Переменные окружения**: Конфиденциальные данные, такие как токены для `Google Sheets API`, имя таблицы и ссылки на посты, загружаются из файла `.env` в `config.py`.
- **Директории**: Настраивается путь к директории для локальных бэкапов данных.
- **Зависимости**: Импортируются все необходимые библиотеки в каждом модуле проекта.

### Архитектура проекта

Проект имеет модульную структуру, где каждый файл отвечает за свою зону ответственности:
- `network.py`: Отвечает за сетевые взаимодействия — получение HTML-кода страниц и подключение к Google API.
- `config.py`: Централизованное место для хранения всех настроек и констант.
- `parsing.py`: Содержит логику обработки сырого HTML и извлечения из него полезных данных.
- `models.py`: Определяет структуру данных (`Data Class`) для хранения информации о каждом товаре.
- `storage.py`: Реализует сохранение обработанных данных в локальный JSON-файл.
- `main.py`: Точка входа в приложение. Инициализирует и координирует работу всех модулей.

### Детальная реализация

- **`network.py`**:
  - Класс `TelegramClient` использует библиотеку `requests` для отправки GET-запросов по URL-адресам из `config.py`. В результате возвращается либо сырой HTML-код страницы, либо ошибка сети.
  - Класс `GoogleSheetsClient` устанавливает соединение с Google Sheets API для дальнейшей записи данных.

- **`parsing.py`**:
  - Класс `TextExtractor` принимает сырой HTML, структурирует его и извлекает текстовое содержимое постов.
  - Класс `PriceParser` обрабатывает извлеченный текст с помощью регулярных выражений (`regex`), разделяя его на логические части: имя товара, цена, флаг и комментарий.

- **`models.py`**:
  - Реализован как `Data Class`, который служит шаблоном для каждого товара (`имя`, `цена`, `флаг`, `комментарий`). Это позволяет работать с удобными линейными списками объектов вместо вложенных словарей. Модель имеет методы для представления данных в виде словаря (для `json`) или списка (для `Google Sheets`).

- **`main.py`**:
  - Оркестрирует весь процесс: запускает сетевые подключения, передает данные между слоями (сеть -> обработка -> хранение/выгрузка) и управляет общей логикой выполнения. Выступает в роли центрального хаба программы.

## 📂 Структура проекта

```text
price_parser/
├── config.py           # Настройки и список ссылок
├── main.py             # Точка входа (запуск пайплайна)
├── models.py           # Модели данных (dataclass Product)
├── network.py          # Клиенты для Telegram и Google Sheets
├── parsing.py          # Логика парсинга и регулярные выражения
├── storage.py          # Сохранение в JSON
├── credentials.json    # Ключи доступа (не пушить в git!)
└── .env                # Переменные окружения
```

## 🛡️ Обработка ошибок
Скрипт устойчив к изменениям:
*   Если лист в Google Таблице не найден — он будет создан автоматически.
*   Если пост в Telegram удален или недоступен — скрипт пропустит его и продолжит работу.
*   Если формат цены нестандартный — эвристический алгоритм попытается найти наиболее вероятное значение.

## 📝 TODO / Планы
- [ ] Добавить выгрузку данных в тг-бота при запросе
---
Developed by Esmien
